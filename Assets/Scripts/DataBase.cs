using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using MongoDB.Driver;

public class DataBase : MonoBehaviour {
	/*MongoDB.Driver.MongoClient db = new MongoDB.Driver.MongoClient("mongodb+srv://glebDyakov:<password>@cluster0.tphcd.mongodb.net/<dbname>?retryWrites=true&w=majorit`y");
	MongoDB.Driver.IMongoDatabase database;
	MongoDB.Driver.IMongoCollection<BsonDocument> collection;
	// Use this for initialization
	void Start () {
		database = client.GetDatabase ("playersdb");
		collection = database.GetCollection<BsonDocument> ("players");
		var document = new BsonDocument {{ "record", 100 }};
		collection.InsertOne(document);
	}
	
	// Update is called once per frame
	void Update () {
		
	}*/
}
